import MediaDetail from '@clientComponents/Media/MediaDetail';
import VideoGrid from '@clientComponents/Media/VideoGrid';

export default function Media() {
	return (
		<main>
			<MediaDetail />
			<VideoGrid />
		</main>
	);
}